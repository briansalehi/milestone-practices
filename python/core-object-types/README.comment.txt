In Python, data takes the form of <i>objects</i>, either built-in objects that
python provides, or objects we create using Python classes or external
language tools such as C extension libraries.

Because objects are the most fundamental notion in Python programming, we'll
start this practices with a survey of Python's built-in object types.
