<i>touch</i> command's main duty is to change file timestamps, but we also
use it to create files.
Many file names can be given to create all at once.

Special characters like / and + should be avoided in file names.
