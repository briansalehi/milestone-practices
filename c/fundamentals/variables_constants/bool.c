#include <stdio.h>
#include <stdbool.h>

int main(void)
{
    bool state = false;

    if (state) {
        printf("state is true\n");
    }
    else {
        printf("state is false\n");
    }

    return 0;
}
