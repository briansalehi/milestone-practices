When a function object returns **bool**, we call it a **predicate**.  
Unary function objects that return bool are unary predicates.  
Predicates are mostly used in sorting and comparison operations.
