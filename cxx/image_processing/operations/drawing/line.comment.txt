The function **cv::line()** draws a straight line from pt1 to pt2 in the image img.

```cxx
cv::line(
    cv::Mat&    img,
    cv::Point   pt1,
    cv::Point   pt2,
    const cv::Scalar&   color,
    int lineType = 8,
    int shift = 0
);
```
