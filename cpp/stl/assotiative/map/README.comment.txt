<p>The difference between the <code>std::map</code> and the <code>std::multimap</code> is that only the latter allows for duplicates, whereas the former can store only unique keys.</p>

<p>To use class <code>std::map</code> or <code>std::multimap</code>, include header: <code>&lt;map&gt;</code></p>
