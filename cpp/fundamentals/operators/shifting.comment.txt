<p>Do not get confused by <b>shift operator</b> <code>operator&lt;&lt;</code> and <code>operator&gt;&gt;</code> with <b>insertion operation</b> to <code>std::cout &lt;&lt;</code> and <code>std::cin &gt;&gt;</code>. They are so different and each of them do something differently.</p>  
