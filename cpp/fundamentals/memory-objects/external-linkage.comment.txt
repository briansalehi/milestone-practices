<p>Variables can be shared between different files of the same project by <i>extern</i> keyword.</p>  

<p>External variables must be declared in each file before use, and it must be initialized only once in only one file. To use an external variable in files other than the one it was initialized, you must declare it using <i>extern</i> keyword but with no initialization.</p>
