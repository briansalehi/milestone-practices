<p>Though, it's the easier way, but it's highly recommended not to use <b>preprocessor directives</b> for variables, as they do not have types and they are just string replacements. There are much better ways to do the same task by <code>constexpr</code>.</p>
