<p>Notice that this function does exactly what was <code>get()</code> function doing, except that it will not break the input stream after a space and will read a complete line until it reaches the <b>newline</b> character.</p>
