<p><code>default</code> keyword can be used to let the compiler generate specified methods automatically just the way it creates a <b>default constructor</b> when you don't declare one.</p>

<p><b>Note</b> that some methods cannot be defaulted, such as <b>overloaded copy constructors</b>, overloaded <code>operator+</code> and etc.</p>
